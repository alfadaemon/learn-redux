import React from 'react';

const Single = React.createClass({
    render(){
        return(
            <div className="single-grid">
                I'm the single view.
            </div>
        )
    }
});

export default Single;